import { initHeaderClient } from '../../src/components/header/client.ts';

/** Initialize header client scripts once on the page. */
export function initHeaderClientScript() {
  initHeaderClient();
}

export { initHeaderClientScript as initHeaderClient };
